{
  "name": "shypixels",
  "version": "1.0.0",
  "private": true,
  "description": "Select any element on any website and apply blur behavior.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "esbuild": "^0.27.3",
    "sharp": "^0.34.5",
    "typescript": "^5.4.0"
  }
}
